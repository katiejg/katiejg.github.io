import React from 'react';
import Nav from '../Nav';
import App from '../App';

export default function Root() {
      return (
            <>
            <Nav />
            <App />
            </>
      );
    }